{
  "pluginAlias": "Porsche",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "headerDisplay": "Adds your Porsche to Apple Home. **First run: sign in above with your Porsche ID** (email, password, and a captcha image if prompted) — after that the plugin runs headless via the refresh token. You can also use the `porsche-auth` CLI. See the README for details.",
  "footerDisplay": "Unofficial, reverse-engineered API. If you hit \"tokens lost\" errors, run `porsche-auth` again.",
  "schema": {
    "type": "object",
    "properties": {
      "vehicleName": {
        "title": "Display name",
        "type": "string",
        "required": true,
        "default": "Porsche",
        "description": "Names every accessory in the Home app and titles the charging dashboard, including its browser tab and home-screen icon."
      },
      "language": {
        "title": "Language",
        "type": "string",
        "required": false,
        "default": "en",
        "description": "Language of the HomeKit tile names.",
        "oneOf": [
          {
            "title": "English",
            "enum": [
              "en"
            ]
          },
          {
            "title": "German / Deutsch",
            "enum": [
              "de"
            ]
          }
        ]
      },
      "vin": {
        "title": "VIN (empty = first vehicle)",
        "type": "string",
        "required": false
      },
      "vehicleModel": {
        "title": "Model (HomeKit device details)",
        "type": "string",
        "required": false,
        "default": "Porsche",
        "description": "Shown as the Model in each accessory's details (e.g. Taycan, Macan)."
      },
      "detailLevel": {
        "title": "Tile scope",
        "type": "string",
        "required": false,
        "default": "essential",
        "description": "\"Essential\" shows only the most important everyday tiles. \"Full\" shows the complete cockpit (all ~55 tiles).",
        "oneOf": [
          {
            "title": "Essential (recommended)",
            "enum": [
              "essential"
            ]
          },
          {
            "title": "Full (everything)",
            "enum": [
              "full"
            ]
          }
        ]
      },
      "vehicleType": {
        "title": "Drivetrain",
        "type": "string",
        "required": false,
        "default": "ev",
        "description": "Controls the energy tiles. \"Electric\" = charge level/range/charging. \"Combustion\" = fuel level/range. \"Plug-in hybrid\" = both. Note: only electric (Taycan) is live-tested.",
        "oneOf": [
          {
            "title": "Electric (EV)",
            "enum": [
              "ev"
            ]
          },
          {
            "title": "Combustion",
            "enum": [
              "combustion"
            ]
          },
          {
            "title": "Plug-in hybrid (PHEV)",
            "enum": [
              "phev"
            ]
          }
        ]
      },
      "spin": {
        "title": "Porsche S-PIN (for unlocking)",
        "type": "string",
        "required": false,
        "description": "4-digit security PIN from the My Porsche app. Only needed for UNLOCKING via Apple Home. Leave empty = the lock can only lock. Stored in plain text in the Homebridge config."
      },
      "pollIntervalMinutes": {
        "title": "Poll interval (min, at least 10)",
        "type": "integer",
        "required": false,
        "default": 15,
        "minimum": 10
      },
      "homeLat": {
        "title": "Home latitude (for 'Car at home')",
        "type": "number",
        "required": false,
        "default": 0
      },
      "homeLon": {
        "title": "Home longitude",
        "type": "number",
        "required": false,
        "default": 0
      },
      "homeRadiusM": {
        "title": "Home radius (meters)",
        "type": "integer",
        "required": false,
        "default": 150,
        "minimum": 10
      },
      "defaultTargetTemp": {
        "title": "Pre-conditioning target temperature °C",
        "type": "number",
        "required": false,
        "default": 21
      },
      "tempMin": {
        "title": "Min. target temperature °C",
        "type": "number",
        "required": false,
        "default": 15
      },
      "tempMax": {
        "title": "Max. target temperature °C",
        "type": "number",
        "required": false,
        "default": 30
      },
      "tempStep": {
        "title": "Temperature step °C",
        "type": "number",
        "required": false,
        "default": 0.5
      },
      "lowBatteryThreshold": {
        "title": "'Battery low' warning threshold %",
        "type": "integer",
        "required": false,
        "default": 20,
        "minimum": 0,
        "maximum": 100
      },
      "serviceWarnKm": {
        "title": "Service warning below remaining distance (km)",
        "type": "integer",
        "required": false,
        "default": 2000,
        "minimum": 0
      },
      "tireDiffThreshold": {
        "title": "Tire pressure warning threshold (bar)",
        "type": "number",
        "required": false,
        "default": 0.3,
        "minimum": 0
      },
      "staleMinutes": {
        "title": "Data considered stale after (min)",
        "type": "integer",
        "required": false,
        "default": 30,
        "minimum": 1
      },
      "heatingAutoOffMinutes": {
        "title": "Heating auto-off (min)",
        "type": "integer",
        "required": false,
        "default": 15,
        "minimum": 1
      },
      "honkAutoOffSeconds": {
        "title": "Honk/flash auto-off (sec)",
        "type": "integer",
        "required": false,
        "default": 3,
        "minimum": 1
      },
      "exposeClimateZones": {
        "title": "Show climate zone sensors",
        "type": "boolean",
        "required": false,
        "default": true
      },
      "exposeTheftSensor": {
        "title": "Show theft sensor",
        "type": "boolean",
        "required": false,
        "default": false
      },
      "pricePerKwhCt": {
        "title": "Electricity price (ct/kWh)",
        "type": "number",
        "default": 0,
        "minimum": 0,
        "description": "Your energy price per kWh in cents. Leave at 0 to hide all cost figures — a made-up price would be worse than none."
      },
      "chargingBonusCt": {
        "title": "Charging bonus (ct/kWh)",
        "type": "number",
        "default": 0,
        "minimum": 0,
        "description": "Discount per kWh deducted from the price, e.g. for tariff-controlled charging. Effective price = price minus bonus."
      },
      "externalPricePerKwhCt": {
        "title": "Price away from home (ct/kWh)",
        "type": "number",
        "default": 0,
        "minimum": 0,
        "description": "Default price for charges away from home. Your home tariff does not apply there — a rapid charger costs several times as much. At 0 the cost stays empty until you enter a price per charge in the dashboard, where this value can also be overridden individually."
      },
      "capacityKwh": {
        "title": "Usable battery capacity (kWh)",
        "type": "number",
        "default": 83.7,
        "minimum": 10,
        "description": "Net usable capacity. Charged energy is derived from the state-of-charge delta times this value. The dashboard measures your real capacity from driving data and shows it next to this setting."
      },
      "dashboardPort": {
        "title": "Charging dashboard port",
        "type": "integer",
        "default": 8099,
        "minimum": 0,
        "maximum": 65535,
        "description": "Port of the built-in charging history dashboard. It binds to ALL network interfaces and has NO password — anyone who can reach this port can read your charging history. Keep it off the public internet. Set to 0 to disable the dashboard entirely."
      },
      "pluggedPollMinutes": {
        "title": "Poll interval while plugged in (min)",
        "type": "integer",
        "default": 1,
        "minimum": 1,
        "maximum": 30,
        "description": "How often to poll while the cable is connected. May go below the 10-minute floor: the car is on mains power, so the 12V concern does not apply. Lower values catch short tariff windows."
      },
      "dayBoundaryHour": {
        "title": "Day starts at (hour)",
        "type": "integer",
        "default": 0,
        "minimum": 0,
        "maximum": 12,
        "description": "Hour at which a new day begins for the statistics. Default 0 splits an overnight charge across both days; set to e.g. 4 so a 22:00-06:00 charge counts towards the evening it started."
      },
      "chargeWindowFrom": {
        "title": "Charging window — start (HH:MM)",
        "type": "string",
        "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
        "description": "Charge only within this window, e.g. 00:30 for a night tariff. The car's own timer knows a departure time but no window. Leave both fields empty to disable — then the plugin never touches charging. The car's instant-charge threshold is always honoured: below it, charging happens whatever the clock says."
      },
      "chargeWindowTo": {
        "title": "Charging window — end (HH:MM)",
        "type": "string",
        "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
        "description": "End of the charging window, e.g. 04:30. A window may span midnight (22:00 to 06:00)."
      },
      "ntfyTopic": {
        "title": "ntfy topic (push notifications)",
        "type": "string",
        "description": "Topic on the ntfy server. Leave empty to disable push. Choose something unguessable — ntfy topics are publicly addressable."
      },
      "ntfyServer": {
        "title": "ntfy server",
        "type": "string",
        "default": "https://ntfy.sh",
        "description": "Push server. Change only if you self-host ntfy."
      },
      "dailyPushHour": {
        "title": "Daily report hour",
        "type": "integer",
        "default": 8,
        "minimum": -1,
        "maximum": 23,
        "description": "Local hour for the daily charging report. Set to -1 to disable."
      },
      "pushOnChargeEnd": {
        "title": "Notify when charging ends",
        "type": "boolean",
        "default": true,
        "description": "Send a push when the car is unplugged, summarising the completed charge."
      }
    }
  },
  "form": [
    "vehicleName",
    "language",
    "vin",
    "vehicleModel",
    "detailLevel",
    "vehicleType",
    {
      "key": "spin",
      "type": "password"
    },
    {
      "type": "fieldset",
      "title": "Polling & Climate",
      "expandable": true,
      "expanded": true,
      "items": [
        "pollIntervalMinutes",
        "defaultTargetTemp",
        "tempMin",
        "tempMax",
        "tempStep"
      ]
    },
    {
      "type": "fieldset",
      "title": "Location \"Car at home\"",
      "expandable": true,
      "expanded": false,
      "items": [
        "homeLat",
        "homeLon",
        "homeRadiusM"
      ]
    },
    {
      "type": "fieldset",
      "title": "Warnings & Thresholds",
      "expandable": true,
      "expanded": false,
      "items": [
        "lowBatteryThreshold",
        "serviceWarnKm",
        "tireDiffThreshold",
        "staleMinutes"
      ]
    },
    {
      "type": "fieldset",
      "title": "Comfort Timers & Sensors",
      "expandable": true,
      "expanded": false,
      "items": [
        "heatingAutoOffMinutes",
        "honkAutoOffSeconds",
        "exposeClimateZones",
        "exposeTheftSensor"
      ]
    },
    {
      "type": "fieldset",
      "title": "Charging History & Costs",
      "expandable": true,
      "expanded": false,
      "items": [
        {
          "type": "help",
          "helpvalue": "<p class=\"help-block\">The plugin records every poll and reconstructs your charging sessions from it. A dashboard on your local network shows them by day, week, month and year.<br><br><b>Costs are opt-in:</b> leave the price at 0 and no cost figures appear anywhere — a made-up price would be worse than none.</p>"
        },
        "pricePerKwhCt",
        "chargingBonusCt",
        "externalPricePerKwhCt",
        "capacityKwh",
        "dashboardPort",
        "pluggedPollMinutes",
        "dayBoundaryHour"
      ]
    },
    {
      "type": "fieldset",
      "title": "Push Notifications (ntfy)",
      "expandable": true,
      "expanded": false,
      "items": [
        {
          "type": "help",
          "helpvalue": "<p class=\"help-block\">Optional. Leave the topic empty to disable push entirely — nothing is sent and no connection is made.</p>"
        },
        "ntfyTopic",
        "ntfyServer",
        "dailyPushHour",
        "pushOnChargeEnd"
      ]
    }
  ]
}
